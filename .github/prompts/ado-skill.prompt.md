---
mode: agent
description: Safe Azure DevOps access (offline/live) — fetch/create work items, test cases, suites.
---

Use the **ADO skill** as defined in `agents/ado-skill.md` — follow its offline/live modes and safety rules exactly (no deletes; bulk operations one-by-one with confirmation; never expose `ADO_PAT`). Offline is the default.
