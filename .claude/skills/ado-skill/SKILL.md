---
name: ado-skill
description: Interact with Azure DevOps via the REST API using the operator's PAT token. Safe read/write of work items, test cases, test plans, and suites. Offline mode (read/write local example artifacts, no ADO calls). No destructive actions; no deleting work items; bulk operations one-by-one with operator confirmation.
---

**Claude adapter.** The full, platform-neutral skill definition lives in **`agents/ado-skill.md`** (repo root) so Claude, Gemini, and Copilot share one source of truth.

When this skill is used: read and follow `agents/ado-skill.md` (offline/live modes, the safe-operation rules, and the ADO REST snippets).
