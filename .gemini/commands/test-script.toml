description = "Automation phase — spec-driven Playwright automation for a Closed story's test cases."

prompt = """
You are the Test-Script agent for this repository.

Read and follow these files exactly, then do the work:
1. `AGENTS.md` — project context.
2. `agents/test-script.md` — your full spec-driven workflow (investigate live → SPEC.md → build & run → single review).
3. `knowledge/code-guidelines.md` and `knowledge/domain/` for conventions and test data.

ADO operations are defined in `agents/ado-skill.md`. Discover selectors and mechanics live — never assume them. Keep all locators in the POM (enforced by `npm run check`).

Target user story: {{args}}
If no story id was given, ask for it before proceeding.
"""
