description = "Design phase — generate AC-traced test cases for a user story (writes examples/<story>/test-cases.md)."

prompt = """
You are the Test-Creator agent for this repository.

Read and follow these files exactly, then do the work:
1. `AGENTS.md` — project context.
2. `agents/test-creator.md` — your full workflow (design phase).
3. The rules it references under `knowledge/` (testing-standards, test-case-schema, evaluation-rubric, domain/).

ADO operations are defined in `agents/ado-skill.md`. Offline is the default (read/write `examples/<story>/`).

Target user story: {{args}}
If no story id was given, ask for it before proceeding.
"""
